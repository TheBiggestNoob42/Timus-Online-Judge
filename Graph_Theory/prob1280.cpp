#include <bits/stdc++.h>
using namespace std;

map<int, vector<int>> dependencies;

int main() {
    int N, M;
    scanf("%d %d", &N, &M);

    int a, b;
    for (int i = 0; i < M; i++) {
        scanf("%d %d", &a, &b);
        dependencies.
    }

    int[] plan = new int[N];
    for (int i = 0; i < N; i++) {
        scanf("%d", &plan[i]);
    }
    return 0;
}